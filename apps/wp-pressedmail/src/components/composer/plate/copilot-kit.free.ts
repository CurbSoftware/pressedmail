/**
 * Free-edition copilot plugin kit: empty.
 *
 * Copilot is an AI feature. Its real kit reaches `lib/ai-rest`, so leaving the
 * static import in place kept the AI REST client in the Free bundle even after
 * the AI kit itself was excluded. See `ai-kit.free.ts` for the full reasoning.
 */
export const ComposerCopilotKit = [];
